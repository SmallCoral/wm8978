# 2026-09-23 工程快照

本快照包含当前 KiCad 工程、项目符号库和封装库、检查及同步脚本、历史检查报告，以及当前中文原理图 PDF。正式入口为 `pcb/WM8978.kicad_pro`，需要 KiCad 10.0 及标准库。

## 当前状态

- SAMD21E18A 与 WM8978 使用独立的 12 MHz、12.288 MHz 时钟，codec 为 I2S 主机。
- 9 处蓝色说明为中文，使用 KiCad 默认字体；已检查导出图的字形及排版。
- 54 个实体器件；14 个电阻与 25 个陶瓷电容均为 0603 英制（1608 公制）。
- C86、C87 的 470 µF / 16 V 电解电容保留 8 mm 罐体封装。
- 54 个原理图与 PCB 封装标识一致，所有连接引脚均有对应焊盘。

## 本次验证

- KiCad 原生 ERC：0 错误、0 警告，见 `erc.json`。
- 新导出网表的 35 项拓扑检查全部通过，见 `netlist.xml` 和 `topology-check.json`。
- PCB 与原理图一致性问题：0 项，见 `drc.json`。
- PCB 已有 89 段走线，尚无铺铜区，仍有 102 个未连接项目。
- USB2 封装内部仍有 4 处孔间距违规：实际 0.1944 mm，小于当前 0.25 mm 规则；需核对原厂机械图与制造要求。

这是开发中的硬件工程快照，尚未完成 PCB 布线，没有配套固件或实板验证。旧方案的独立参考图与历史报告保留各自日期和用途，不代表当前设计。

## 打包范围

ZIP 从本次 Git 提交生成，包含所有受版本控制的工程文件。沿用 `.gitignore`，排除本机历史缓存、编辑器状态、锁文件及旧编译输出；不打包 `.git` 目录。GitHub 仓库保留完整 Git 提交历史。
